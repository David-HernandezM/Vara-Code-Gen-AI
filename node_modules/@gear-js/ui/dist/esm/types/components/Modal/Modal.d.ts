import { ReactNode, ReactPortal } from 'react';
import styles from './Modal.module.scss';
type Props = {
    heading: string;
    close: () => void;
    children?: ReactNode;
    footer?: ReactNode;
    className?: string;
    size?: 'normal' | 'large';
};
declare const Modal: ({ heading, close, children, footer, className, size }: Props) => ReactPortal | null;
export { Modal, styles as modalStyles };
export type { Props as ModalProps };

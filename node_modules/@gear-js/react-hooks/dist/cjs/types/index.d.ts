import { AlertType, AlertOptions, TemplateAlertOptions, AlertInstance, AlertTimer, AlertTemplateProps, AlertContainerFactory, DefaultTemplateOptions } from './alert';
type ProviderProps = Omit<React.ProviderProps<never>, 'value'>;
type Entries<T> = {
    [K in keyof T]: [K, T[K]];
}[keyof T][];
export { AlertType };
export type { AlertOptions, TemplateAlertOptions, AlertInstance, AlertTimer, AlertTemplateProps, AlertContainerFactory, DefaultTemplateOptions, ProviderProps, Entries, };

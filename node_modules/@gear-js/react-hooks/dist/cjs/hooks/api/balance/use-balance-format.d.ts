import { formatBalance } from '@polkadot/util';
import { BigNumber } from 'bignumber.js';
declare function useBalanceFormat(): {
    balanceMultiplier: BigNumber;
    decimals: number;
    getChainBalanceValue: (value: string | number) => BigNumber;
    getChainGasValue: (value: string | number) => BigNumber;
    getFormattedBalanceValue: (value: string | number) => BigNumber;
    getFormattedGasValue: (value: string | number) => BigNumber;
    getFormattedBalance: (balance: Exclude<Parameters<typeof formatBalance>[0], undefined>) => {
        value: string;
        unit: string;
    };
};
export { useBalanceFormat };
